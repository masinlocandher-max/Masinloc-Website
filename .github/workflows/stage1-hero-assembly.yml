name: Assemble Stage 1 hero

on:
  push:
    branches:
      - stage-1-home-about
    paths:
      - '.stage1-upload/**'

permissions:
  contents: write

jobs:
  assemble:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: stage-1-home-about
      - name: Check all chunks are present
        id: chunks
        shell: bash
        run: |
          count=$(find .stage1-upload -maxdepth 1 -type f -name 'chunk-*' 2>/dev/null | wc -l)
          echo "count=$count" >> "$GITHUB_OUTPUT"
      - name: Assemble verified original-photo derivative
        if: steps.chunks.outputs.count == '4'
        shell: bash
        run: |
          mkdir -p assets
          cat $(find .stage1-upload -maxdepth 1 -type f -name 'chunk-*' | sort) | base64 -d > assets/masinloc-stage1-hero.avif
          echo '2ebad1a8a4250d8b23f021189bf8d800c7e6956593a87d2f6ed453cf58845353  assets/masinloc-stage1-hero.avif' | sha256sum -c -
      - name: Commit assembled hero
        if: steps.chunks.outputs.count == '4'
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm -rf .stage1-upload
          git add assets/masinloc-stage1-hero.avif .stage1-upload
          git commit -m 'Add approved Masinloc Stage 1 hero photo'
          git push origin HEAD:stage-1-home-about
