# Build mode

This repository is developed stage by stage. Active development happens on `agent/*` branches with Vercel preview deployments disabled. GitHub site-integrity checks remain the required validation gate during development. Production deployment happens only after a finished stage is reviewed and merged to `main`.
